Features area where the app owner can either create a single page with all their features on or they can have sections that describe some of the features and then when you click on those features it goes to a full feature page. In the demo, we want it to go, we want to have two blocks to go to feature one, feature two, and those two pages need to be built out to have more in-depth info about the feature. 